{
  "name": "bindsql-for-eclipse",
  "private": true,
  "scripts": {
    "build:validator": "node scripts/build-validator.mjs",
    "test": "node --test test/*.test.mjs"
  },
  "devDependencies": {
    "libpg-query": "15.6.3"
  }
}
