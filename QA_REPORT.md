# 内部品質改善・検証記録

## 追加改善（2026-09-23、入力の忠実性・長いSQL・操作状態）

開始時点の未コミット変更を保持し、263件の既存テストに22件の回帰テストを追加しました。今回の変更は配布HTMLのメインスクリプトと、新規テスト・この記録です。

改善内容：

- `U&'!65E5!672C' UESCAPE '!'`のようなUnicode文字列を、エスケープ指定ごと一つの値として保持します。従来は指定全体を通常の文字列に変え、値の意味を変えていました。名前付き・位置バインド、`sql=[...] params=[...]`形式を確認しています。
- 明示的なバインド欄で、改行を挟んだ文字列の継続とUESCAPE指定を保持します。通常文字列・E文字列・ビット列・16進文字列を検証。未完了の引用や余分なSQL文は、既存の変換エラーまたは文字列データとして扱います。
- `first`、`next`、`row`、`rows`、`set`、`values`、`between`、`by`を列名として使ったJSON演算子を、位置バインドと誤認しないよう修正。`FETCH FIRST/NEXT`、`GROUP/ORDER/PARTITION BY`、`BETWEEN`、ウィンドウの`ROWS`に続く位置バインドは引き続き置換します。
- JSON演算子の右辺に単項演算子がある場合の判定を補正しました。
- 暗黙のバインド定義になり得る末尾部分を先に絞り込み、多数の条件式ごとにSQL先頭から再走査していた処理を削減しました。明示的なバインド見出しの有無の両方で、1,200条件が欠落せず、文脈の再走査が2回以下になることを自動テストで確認します。
- クリップボードの代替コピー処理で、逆向きの文字選択と縦横のスクロール位置も復元します。
- 日本語変換中にページを離れて履歴から戻った場合、変換中の状態が残って以後のSQL更新を妨げないよう修正しました。
- 出力が同じ場合のtextareaへの重複書き込みを省きました。

検証記録：

- `npm test`：285件成功・0件失敗。追加SQLケースは同梱PostgreSQL 15パーサーで期待SQLと構文木を比較しました。
- `npm run build:validator -- --check`、`git diff --check`：成功。
- 開始時点と比較し、**メインスクリプト以外のHTML全バイトが一致**。DOM・CSS・静的文言・ボタン・CSP・Worker・WASM・ライセンス、および既存MESSAGES定義を維持しています。
- Codex内蔵ブラウザでlocalhost経由の配布HTMLを確認。Unicodeの値、継続E文字列、JSON演算子、ウィンドウの位置バインドについて実Worker/WASMのチェック成功を確認し、Unicodeを含むコピー内容と出力SQLの一致を確認しました。未完了のUnicode引用ではコピーが無効になり、正常入力への復旧と既存のINSERT列数診断も確認しました。
- 3,000条件と位置バインド1個を含むSQLの変換時間を、ローカルNodeの代替DOM環境で各3回測定。中央値は開始時点の約14.38秒から約0.136秒に短縮しました。ブラウザの描画・WASM検証時間を含まない、実行環境の負荷に依存する参考値です。
- SVGアイコンのクリックも実ブラウザで点検。既存のCSSによって正常に動作しており、この箇所の変更は不要でした。
- 診断位置ボタンで出力欄へのフォーカス移動、クリア後の入力欄への復帰とコピー無効化を確認しました。確認時のブラウザエラー・警告ログは0件です。

Unicodeエスケープと文字列の継続は[PostgreSQL 15の字句仕様](https://www.postgresql.org/docs/15/sql-syntax-lexical.html)と同梱パーサーで確認しています。今回も実DBでの実行、他ブラウザ、OSの実IME操作、`file://`での操作は検証範囲に含めていません。

## 追加改善（2026-09-23、UI・文言・ボタンの追加なし）

開始時点の未コミット変更を保持し、233件の既存テストに30件を追加しました。**263件成功・0件失敗**です。

今回修正した内容：

- JOINのON、HAVING、SELECTのカンマ、演算子の直後に続く条件式を、暗黙のバインド定義として削除しないようにしました。コメントを挟むWHERE/ANDも同様です。条件式の`1=1`を不足しているバインド値として流用するケースも防ぎます。
- `t.where ? 'key'`のようなキーワードと同名の列では、`?`をJSON演算子として保持します。`t.array[:upper]`のスライス境界も、名前付きバインドに置換しません。修飾名の間にある空白・コメントにも対応します。
- TRIMのBOTH/LEADING/TRAILINGの直後にある位置バインドを認識します。
- Hibernateロガーの同一行にあるVALUES、CALL、EXPLAIN、括弧で囲まれたSELECTを、SQL本体として取得します。
- OVER、EXTRACT、SUBSTRING等の直後の空白で括弧の文脈判定が失敗し、関数内の句を不自然に改行していた処理を修正しました。
- 全角空白等の非ASCII文字や垂直タブだけの入力を、空入力として構文チェックから除外しません。PostgreSQLの解析器に渡して診断します。
- 整形結果が同じ編集では、実行中の解析や既存の診断を再利用します。解析失敗後の再試行は維持します。
- Workerを再作成するたびに約0.9MBのWASMをBase64から復号していた処理を、復号済みバイナリの再利用に変更しました。Workerへはコピーを転送するため、取消・クリア後の再起動でも元のバイナリが失われません。

検証内容：

- `node --test test/*.test.mjs`：263件成功。追加したSQLケースは同梱PostgreSQL 15パーサーで期待SQLと構文木を比較しました。`LIMIT ALL`、`OFFSET ... ROWS`、キーワード名の列、ワイルドカードと暗黙バインドの組み合わせも回帰確認しました。
- `npm run build:validator -- --check`、`git diff --check`：成功。
- 開始時点との比較で、メイン処理・検証制御・生成されたWorkerの3スクリプト以外はバイト単位で一致。DOM・CSS・静的文言・ボタン・CSP・WASM本体・ライセンス・既存MESSAGES定義を維持しています。
- Codex内蔵ブラウザでlocalhost経由のHTMLを操作。JOIN条件の保持、配列スライス、JSON演算子、TRIM、値不足時のコピー停止、列数エラーとエラー位置の選択、Hibernateログ、エラー後の復旧を確認しました。
- 同ブラウザでコピー内容を取得し、さらにクリア後の貼り戻しで出力SQLとの一致を確認しました。確認時のブラウザエラー・警告ログは0件でした。
- Worker再作成時のメインスレッド側準備処理を、Nodeの代替Worker環境で測定しました。初回復号を除く8回の再作成を5セット行った中央値は、1回あたり約232msから約0.52msでした。WASMのコンパイル・解析時間を含まない参考計測で、実ブラウザの処理全体の時間を示すものではありません。

`file://`での直接操作はブラウザ操作ツールのURLポリシーで拒否されたため未検証です。実DBでの実行、他ブラウザ、OSの実IME操作についても今回の検証結果には含めていません。

## 追加改善（2026-09-22、UI・文言・ボタンを増やさない品質改善）

今回の開始時点の197件に回帰テスト36件を追加し、**233件成功・0件失敗**を確認しました。開始時点の未コミット変更を保持し、配布HTMLのメイン処理と回帰テストを更新しています。

| 領域 | 追加で修正した問題 |
| --- | --- |
| SQLとログの区別 | `CALL`・`COPY`・`EXPLAIN`・`PREPARE`・`NOTIFY`等の文字列に含まれるログ風の文言を、本物のログと誤認してSQL本文を切り取る問題を修正。SQL開始判定を整形用キーワード辞書から独立させた。 |
| 複数行のログ値 | MyBatisで値の先頭行だけを成功扱いする問題を修正。MyBatis・Hibernateの2形式・Spring JDBCの複数行値、空行、前後空白、引用符を保持。未完了の値は既存の変換エラーとして扱い、コピーを停止する。 |
| 曖昧なログ境界 | 未完了の値の後にSQL開始らしい行が現れても、それが値の続きである可能性を残す。後続SQLで未完了エラーを消さず、別のSQLが成功扱いになることを防止。 |
| バインドと演算子 | `AT TIME ZONE ?`、単項演算子が続くJSON演算子、`??`、引用付きスキーマの`OPERATOR(...)`を区別。名前が`zone`の列も保持。 |
| 配列 | コメントを挟む`ARRAY`、多次元配列、配列内の別の配列スライスを区別。スライスの上限を名前付きバインドと誤認しないよう修正。 |
| 引用された値 | `params=[...]`内のドル引用、末尾バックスラッシュを含む通常の文字列、改行で継続するE文字列を正しく分割。継続行にあるカンマ・`?`・`:name`も文字列として保持。 |
| Unicode | PostgreSQLで識別子になり得るUnicode空白を、JavaScriptのtrimで削除して列自体を消す問題を修正。 |
| 日本語入力・表示 | IME変換中にも透明textareaの背面表示を更新。compositionstartがない場合も古い出力のコピーを停止。末尾改行の表示層を補正し、入れ子コメントの色分けをSQLの読み取り規則と統一。 |
| 処理量 | `?`や配列括弧ごとにSQL先頭から走査する処理を減らし、コメントを除いた判定用文字列の処理を共通化。 |

検証内容：

- `npm test`：233件成功。新規ケースも実際のPostgreSQL 15パーサーで構文木を比較。
- `npm run build:validator -- --check`：成功。埋め込みWorker・WASM・ライセンスに差分なし。
- 開始時点のHTMLとの比較で、**メイン処理以外の全バイトが一致**。DOM・CSS・ボタン・CSPは同一で、静的な日本語文言も一致。
- 実ChromeでループバックHTTP経由のHTMLを操作。タイムゾーンのバインド、複数行ログの改行保持、実Worker/WASMによる構文・列数チェック、エラー位置の文字選択とフォーカス、未完了ログのコピー停止、正常入力への復旧、クリアを確認。
- コピー成功の表示をChromeで確認。ブラウザ操作ツールの仮想クリップボードとページのクリップボードは共有されず、実クリップボードの内容一致は確認できなかった。内容一致・失敗時の復旧は既存の自動テストで検証。
- 配列添字4,000個の入力について、ローカルNodeでの3回の中央値は77msから52ms。環境依存の参考計測であり、ブラウザの応答時間を保証する数値ではない。

MyBatisの型終端は[公式のログ生成実装](https://mybatis.org/mybatis-3/xref/org/apache/ibatis/logging/jdbc/BaseJdbcLogger.html)を参照。E文字列の継続と識別子は[PostgreSQL 15の字句仕様](https://www.postgresql.org/docs/15/sql-syntax-lexical.html)および同梱パーサーで確認しました。

今回も実DBの実行結果、`file://`での操作、他のブラウザ、OSの実IME候補選択までは検証していません。型終端やログ区切りそのものを値に含む非構造化ログの完全な復元は保証しません。

以下は、今回の追加改善より前の検証記録です。

---

実施日：2026-09-22

対象：`bind-sql-for-eclipse-postgresql.html`（旧BusinessUI版）

改名後の再検証：`npm test` は **197件成功、0件失敗**、`npm run build:validator -- --check` も成功しました。旧HTMLに対する重複検証53件を対象から外し、現在配布する単一HTMLに検証対象を統一しています。改名前後のHTMLのSHA-256は一致しています。以下の250件という記録は、2つのHTMLを対象としていた品質監査時の結果です。

## 判定

修正前には、SQLの構文や値を変えてしまう不具合が複数ありました。2回の監査で再現ケースをテストに追加して修正しました。品質監査時の自動テストは **250件成功、0件失敗** でした。前回の181件から、追加監査で69件増やしています。

これは商用提供を保証する判定ではありません。実ブラウザー、実際のクリップボード権限、実DB・実ログによる受け入れ試験は未実施です。特に「構文が通る」と「意図どおり実行できる」は区別する必要があります。

## 修正内容

| 領域 | 修正した問題 |
| --- | --- |
| SQL整形 | `<<`・`>>`などの複合演算子、関数の名前付き引数 `:=`、`ASC`・`ASCII`、Unicodeを含む識別子の破損を修正。 |
| SQLの意味の保持 | `2026-09-07`という減算式を日付文字列に勝手に変換しないよう修正。日付のバインド値は従来どおり文字列化。 |
| 文字列・コメント | 文字列内の改行・CR、改行を挟む文字列結合、入れ子のコメント、Unicodeのドル引用タグ、`U&`文字列を保護。 |
| 一時置換トークン | ユーザーのSQLや値が内部トークンと一致しても、内容を削除・誤復元しないよう修正。 |
| バインド判定 | `DISTINCT ?`、コメントの前後の `?`、JSON演算子、連続するプレースホルダーと演算子、`$`を含む名前を修正。 |
| 配列 | 空白のある配列添字でも置換。配列スライスのコロンを名前付きバインドと誤認しないよう修正。 |
| 入力の分離 | SQL中の列名 `params`、文字列内の `params:`、コメント中のログ形式文字列、WHERE/AND/OR直後の条件式を誤って削除しないよう修正。 |
| 値の読み取り | JSON内のカンマによる分割、ログの文字列値に含まれる引用符・前後の空白の消失を修正。 |
| 複数のログ | MyBatis・Hibernateで前のSQLの値を引き継がないよう修正。Spring JDBCも同一形式内の最後のSQLとその後の値を組み合わせるよう修正。 |
| 複数行のログ | HibernateのSQL見出し直後の改行、Spring JDBCの配列・引用符内の `]` を扱えるよう修正。 |
| 不正なバインド | 重複定義、0番、JavaScriptで正確に表せない番号による黙った上書き・無視を防止。既存の余剰バインド診断を使用。 |
| コピー | 入力編集を挟んだ非同期コピーの重複実行、古い結果による成功表示を防止。失敗時の復旧・フォーカス復元を検証。 |
| 構文チェック | 古い解析処理を中断し、最新入力が待たされる問題を修正。不正な応答、メッセージの復号失敗、画面離脱、再試行を処理。完了済みWorkerは再利用。 |
| 大量入力 | `?`の判定結果を再利用し、連続する演算子判定の再帰的な再計算を解消。 |
| 負数の置換 | `-?`に負数を置換して`--`コメントになり、SQLが消える問題を修正。値と周囲のトークンを分離。 |
| リテラル | 空文字、複数行、`E`・`N`・`B`・`X`・ドル引用、JSON形式のエスケープを処理。CR・バックスラッシュを含む生成値はE文字列にして、textareaの改行正規化で意味が変わる問題を防止。 |
| 不正入力の扱い | 未知のバインド行・未完了の引用を黙って無視せず変換エラーとして扱う。引用された値の後ろの文字列を追加のSQLとして展開しない。 |
| ログの境界 | 値やSQL本体中の`Preparing:`・`Hibernate:`等をSQL開始と誤認する問題を修正。3形式のログを共通の処理で読み、形式が混在しても入力順で最後のSQLレコードを選択。 |
| 整形の保護 | 整形の前後でSQLトークンを比較し、内容が変化した場合は変換済みSQLの整形前の内容を保持。別のSQLを黙って出力しない。 |
| 日本語入力 | IMEの変換確定までSQL変換を保留し、その間は古いSQLをコピーできないようにする。確定直後の重複inputイベントを抑制。 |
| 例外からの復旧 | 予期しない変換例外が発生しても古い出力をコピー対象に残さず、次の入力で復旧。構文チェック失敗後は同じ内容の再貼り付けでも再試行。 |
| 構文チェック拡張 | `MERGE ... INSERT`の列数・重複列を既存のINSERT診断で検査。不正なWorker要求・矛盾した成功応答を正常結果として扱わない。 |
| 構文木の処理 | 再帰と大量引数の展開を廃止。広い／深い構文木によるJavaScriptのスタック上限エラーを防止。大量診断のUnicode位置変換をまとめて処理。 |
| 処理量の削減 | 名前付きバインドを一括置換。SQL行ごとの不要な再解析、括弧の先頭からの繰り返し走査、不要な配列生成を削減。 |

## 検証方法と結果（品質監査時）

```sh
npm test
```

- 元HTMLとBusinessUI版の既存検証器テスト106件、入力から出力までの71件、構文チェック制御6件、アプリの異常系6件、SQLの内容保持43件、検証器の異常系5件、ログ境界10件、結合テスト3件。合計250件。
- BusinessUI版の埋め込みJavaScriptをそのままNodeのVMで実行。DOM、タイマー、クリップボード、Workerとの境界はテスト用の代替実装を使用。
- SQL出力をPostgreSQL 15の実パーサーで読み、位置情報を除く構文木が期待SQLと一致することを検証。構文エラーの有無だけの確認ではありません。
- 文字列・Unicode・特殊記号の80通りの生成ケースを含むテスト、大量の2,000行VALUES、6,000個の `?` を含むケースを検証。
- 埋め込みWASMは実際に動作させ、外部への取得を禁止した状態で正常系・構文エラー・初期化失敗・再試行を確認。
- 両HTMLの検証器・ライセンスの埋め込み内容をビルド元と照合。
- 固定シードで生成した160通りの入れ子式、空白・タブ・改行の変種、整形を繰り返しても結果が変化しないことを確認。
- 2,500個の名前付きバインド、1,800件のUnicode付き診断、15万要素／1万5千段の構文木を検証。後者は構文木の処理限界を調べる合成データであり、その深さのSQLをPostgreSQLが受理することを示すものではありません。
- 結合テストではHTMLに埋め込まれた3つのスクリプトを接続し、変換→WASMによる構文チェック→コピーの一致、構文エラーからの復旧、起動中のクリアを確認。Workerのデータ転送はstructuredCloneで再現し、実際にバイナリを渡しています。
- 故障注入テストで、整形器が別のSQLを返した場合の保護と、変換処理で例外が発生した場合のコピー停止・復旧を確認。

ローカルNodeでの単発計測では、名前付きバインド1,500個の変換が約1,155ms→532ms、500文の入力が約647ms→75msでした。環境・JIT・GCの影響を受ける参考値で、ブラウザーでの応答時間を保証する数値ではありません。

現在の配布HTMLを検証・再生成するコマンドは以下のとおりです。

```sh
npm run build:validator -- --check --target bind-sql-for-eclipse-postgresql.html
npm run build:validator -- --target bind-sql-for-eclipse-postgresql.html
```

現在の配布HTMLの検証器ソースは `src/sql-validation-worker-business.js` です。ビルドとテストの対象は、改名後の単一HTMLに統一しています。

## 変更しなかったもの（品質監査時）

UIのDOM、CSS、ボタン、表示文言を追加・変更していません。今回の修正前スナップショットと比較し、メイン処理・構文チェック制御・検証器Workerの3スクリプト以外がバイト単位で一致すること、日本語の表示文言と既存のMESSAGES定義が一致することを確認しました。CSS・JavaScript・WASMは引き続きHTMLに内包しています。

品質監査時点では、比較用の旧HTMLは変更していませんでした。

SHA-256：`ad5d31e65e53ea277840ecd75a6ffa5ea0da327ab0b283f9b62dd2e33bace928`

既存のユーザー編集がある `REPORT.md` も変更していません。

## 残る検証範囲・制約

- この環境では `file://` のブラウザー操作が制限されているため、実ブラウザーでの操作・描画確認は未実施です。Nodeの代替DOMによる確認と区別してください。
- 実DBに接続しないツールなので、テーブル・列の存在、型変換、権限、制約、実行結果は保証しません。PostgreSQL 15の構文と静的に判断できる列数を確認します。
- `?`や`:name`はPostgreSQLの演算子・配列記法と曖昧になり得ます。`a[:upper]`は配列スライスとして扱い、添字に名前付きバインドを使う場合は `a[(:upper)]` のように括弧で区別します。
- Javaの非構造化ログは値の型や区切り情報を失う場合があります。型付き文字列中に値の区切りそのものが含まれる場合、Hibernate/Springで文字列の `null` とSQL NULLを区別できない場合、複数スレッドのSQLと値が交互に出る場合の完全な復元は保証しません。形式の混在には対応しましたが、失われたスレッド情報・型情報を推測して完全復元することはできません。
- トークン比較と構文木比較はSQL内容の破損を防ぐ検査です。JDBCの型付け、実DBの関数オーバーロードやサーバー設定まで含めた実行結果の同一性を保証するものではありません。
- 実データのログ、ブラウザーごとのクリップボード権限、長時間利用、さらに大きい入力での受け入れ試験が、有料提供の判断には必要です。
